# 🧠 llmstruct AI Workflow Rules (TEMP)

## Основные режимы (context tags):
- [discuss] — обсуждение, планирование
- [meta] — работа с инструментами, мета-обсуждения
- [code] — программирование
- [debug] — отладка
- [docs] — документация
- [test] — тестирование
- Комбинируй режимы: [code][debug], [discuss][meta] и т.д.

## Базовые правила для AI:
- Всегда сверяйся с data/ai_workflow.json для этапов, чек-листов и шаблонов
- Для подробных правил и best practices см. .cursor/rules/ (если есть)
- Логируй все ключевые действия, ошибки и переключения режимов в event_log/meta-log
- При потере фокуса — восстанови по meta-log и сверяйся с ai_workflow.json
- При переключении задач/режимов — обнови llm_context и attention_marker
- Если не хватает информации — ищи в .cursor/rules/ и документации (docs/)

## Минимизация деградации контекста:
- Не дублируй детали — только базовые правила, детали по ссылкам
- Регулярно сверяйся с ai_workflow.json и meta-log
- Делай явные напоминания, где искать что

## Ссылки:
- Основной workflow: data/ai_workflow.json
- Тематические правила: .cursor/rules/
- Документация: docs/ONBOARDING_LLMSTRUCT.md, docs/BEST_PRACTICES_LLMSTRUCT.md

# Known AI Pitfalls (Ошибки и ограничения AI)

- Не проверяй/не архивируй файлы до merge PR и git pull develop — иначе можно потерять данные или работать с устаревшими файлами.
- Всегда сверяйся с ai_workflow.json и meta-log перед анализом структуры или логов.
- Если не уверен в актуальности файлов — сначала делай git pull и сверяйся с develop.
- Lessons learned по ошибкам фиксируй в insights.json и event_log.
- Все новые ошибки/ограничения AI должны фиксироваться здесь и в соответствующих логах/insights.

(Подробнее — см. insights.json, ai_workflow.json, event_log) 