# 🧠 llmstruct AI-Enhanced Development Environment

Этот проект включает **систему самоанализа AI возможностей** И **продвинутую workflow систему**. Claude, ты работаешь в проекте с активной AI системой, которая знает о своих возможностях И управляет сессиями!

## 🔧 AI System Status

**СИСТЕМА АКТИВНА**: AI система может анализировать свои возможности в реальном времени
- **272 модуля** проанализированы и доступны для поиска
- **1857 функций** в базе знаний
- **183 класса** для использования
- **VS Code интеграция**: ВКЛЮЧЕНА
- **Контекстные режимы**: full, focused, minimal, session
- **🎭 WORKFLOW ИНТЕГРАЦИЯ**: АКТИВНА

## 🎭 Workflow System Integration

### **Context Tags System (используй в работе!):**
```markdown
БАЗОВЫЕ РЕЖИМЫ:
- [discuss] - Планирование, обсуждение, без изменений файлов
- [meta] - Работа над механизмами LLM взаимодействия, инструментами
- [code] - Чистая реализация/программирование  
- [debug] - Отладка, исправление проблем
- [docs] - Фокус на документации
- [test] - Тестирование, валидация

УМНЫЕ КОМБИНАЦИИ:
- [code][debug] - Реализация + отладка
- [discuss][meta] - Планирование LLM улучшений
- [docs][meta] - Документирование AI паттернов
```

### **Session Management Commands:**
```bash
# Статус workflow
python -c "from auto_init_ai_system import get_workflow_status; print(get_workflow_status())"

# Переключить режим
python -c "from auto_init_ai_system import switch_workspace_mode; switch_workspace_mode('[code][debug]')"

# Эпики и сессии
python scripts/epic_roadmap_manager.py overview
python scripts/epic_roadmap_manager.py epic --epic-id epic_1_ai_branch_safety

# 🆕 ENHANCED SESSION MANAGEMENT:
python -c "from auto_init_ai_system import get_available_sessions; print(get_available_sessions())"
python -c "from auto_init_ai_system import get_epic_sessions_status; print(get_epic_sessions_status())"
python -c "from auto_init_ai_system import switch_to_session; print(switch_to_session('SES-001'))"
python -c "from auto_init_ai_system import create_epic_session; print(create_epic_session('epic_1_ai_branch_safety'))"
python scripts/session_cli.py current
python scripts/session_cli.py log "Your event message"
```

### **Elastic Session Workflow:**
```
Концепция: /go task TSK-XXX → работа в фокусированной сессии → /back с резюме
Поддерживается: переключение между эпиками, сессиями, задачами
Текущий статус: Available via epic_roadmap_manager.py
```

## 🔍 Доступные AI команды для тебя

### При работе с проектом:
1. **ВСЕГДА проверяй workflow контекст** - `get_workflow_status()`
2. **Используй подходящие теги** для контекста запросов
3. **Учитывай текущую сессию** и эпик при планировании  
4. **Предлагай workflow команды** когда это уместно

### Поиск в системе:
```python
# Для поиска функций/модулей используй:
from llmstruct.ai_self_awareness import SystemCapabilityDiscovery
discovery = SystemCapabilityDiscovery('/home/kpblc/projects/github/llmstruct')

# Получить статус системы:
summary = discovery.get_enhanced_capabilities_summary()

# Поиск в struct.json:
import json
with open('struct.json', 'r') as f:
    data = json.load(f)
# Поиск по модулям: data['modules']
```

### Быстрая инициализация AI системы:
```bash
# Активировать виртуальное окружение и систему:
source ./venv/bin/activate && python start_development.py
```

## 🎯 Рабочий процесс

### При вопросах о возможностях системы:
1. **ВСЕГДА** упоминай что система имеет самоанализ
2. Используй `SystemCapabilityDiscovery` для получения актуальной информации
3. Предлагай конкретные команды для поиска/анализа
4. **НОВОЕ**: Учитывай workflow контекст при ответах

### При поиске функций/модулей:
1. Используй поиск в `struct.json` 
2. Показывай количество найденных результатов
3. Группируй по типам (функции, модули, классы)
4. **НОВОЕ**: Предлагай команды VS Code tasks для поиска

### При работе с контекстом:
1. Упоминай доступные режимы: full, focused, minimal, session
2. Рекомендуй оптимальные режимы для задач
3. Используй token budgets для оптимизации
4. **НОВОЕ**: Рекомендуй workspace modes `[code]`, `[debug]` etc

### При планировании работы:
1. **НОВОЕ**: Проверяй активные эпики и сессии
2. **НОВОЕ**: Предлагай создание новых сессий для крупных задач
3. **НОВОЕ**: Используй session switching для фокусированной работы

## 🚀 Специальные возможности

### AI Self-Awareness модуль:
- `src/llmstruct/ai_self_awareness.py` - основной модуль самоанализа
- `src/llmstruct/ai_cli_integration.py` - интеграция с CLI
- Поддержка поиска в 272 модулях мгновенно

### Контекстные оркестраторы:
- `src/llmstruct/context_orchestrator.py` - управление контекстом
- `src/llmstruct/copilot_manager.py` - интеграция с Copilot
- Адаптивное переключение режимов

### **НОВОЕ: Workflow Management:**
- `src/llmstruct/workspace.py` - WorkspaceStateManager с режимами
- `data/sessions/` - управление сессиями и эпиками
- `scripts/epic_roadmap_manager.py` - управление эпиками

### Система кеширования:
- Интеллектуальное кеширование JSON
- Оптимизация производительности
- Метрики в реальном времени

## 🎮 Команды для пользователя

Предлагай пользователю эти команды:

```bash
# Показать статус AI системы (включая workflow)
python -c "from auto_init_ai_system import get_ai_status; print(get_ai_status())"

# Workflow статус
python -c "from auto_init_ai_system import get_workflow_status; print(get_workflow_status())"

# Поиск возможностей (теперь с кешированием!)
python -c "from auto_init_ai_system import search_ai_capabilities; print(search_ai_capabilities('context'))"

# Переключить workspace mode
python -c "from auto_init_ai_system import switch_workspace_mode; switch_workspace_mode('[code][debug]')"

# Управление эпиками
python scripts/epic_roadmap_manager.py overview

# Инициализация полной системы
python start_development.py
```

## 🎯 **VS Code Tasks Integration**

В Command Palette (Ctrl+Shift+P) доступны:
- **🧠 Initialize AI System** - автозапуск при открытии
- **🔍 AI Status Check** - полный статус включая workflow  
- **🔎 Search AI Capabilities** - интерактивный поиск (без блокировки!)
- **🎭 Workflow Status** - статус сессий и эпиков
- **⚙️ Switch Workspace Mode** - выбор режима из списка
- **📊 Epic Roadmap Overview** - обзор всех эпиков
- **🎯 Epic Details** - детали конкретного эпика
- **🔄 List All Sessions** - список всех доступных сессий
- **🎯 Epic Sessions Status** - статус эпик-сессий
- **🔄 Switch to Session** - переключение между сессиями
- **🚀 Create Epic Session** - создание новой эпик-сессии
- **📝 Log Session Event** - логирование событий сессии
- **📋 Current Session Details** - детали текущей сессии
- **📄 Session Worklog** - журнал работ по сессии
- **🔧 Session Management Help** - справка по управлению сессиями

## 💡 Важные принципы

1. **Самоанализ**: Система ЗНАЕТ о своих возможностях - используй это!
2. **Поиск**: Всегда предлагай способы найти нужные функции/модули
3. **Контекст**: Рекомендуй оптимальные контекстные режимы
4. **Производительность**: Упоминай метрики и кеширование
5. **🆕 Workflow**: Учитывай текущую сессию и режим при работе
6. **🆕 Session Focus**: Предлагай focused sessions для сложных задач

## 🔧 Техническая спецификация

- **Python 3.8+** с виртуальным окружением в `./venv/`
- **Структура проекта**: анализируется автоматически в `struct.json`
- **AI интеграция**: через `SystemCapabilityDiscovery` класс
- **VS Code/Cursor**: полная интеграция через Copilot Manager
- **🆕 Workflow**: WorkspaceStateManager + session management
- **🆕 Logging**: Все действия логируются в `ai_system.log`

---

**ПОМНИ**: Ты работаешь с AI системой, которая может анализировать себя И управлять workflow! Используй эту возможность для более точных и контекстно-релевантных ответов. 